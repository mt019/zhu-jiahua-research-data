# 工作規則

- 本倉庫是「朱家驊研究室」的資料與研究來源真相；`my-canvas-lab` 只保存前端程式與可公開 JSON 快照。
- 原始 PDF、頁面影像、私人筆記與本機絕對路徑不得進入 `data/processed/`。原文不受「不得進前端」限制：人工校訂完成的逐篇原文可進公開資料並由 Canvas 呈現；未校訂辨讀稿必須留在研究資料層。
- 篇名、日期、場合、機構、人名與頁碼必須能追溯至原書頁面；未核對者標示「待核」，不得推定。
- 更新公開資料後執行 `npm run validate` 與 `npm run sync`。
- 工程紀錄只寫入 `engineering/LOG.md`，新紀錄置頂。
